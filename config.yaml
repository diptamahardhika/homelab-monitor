port: 9876

servers:
  - name: Localhost
    # Use host.docker.internal to reach the host machine from inside the container.
    # On Linux, add `extra_hosts: - "host.docker.internal:host-gateway"` to your compose service.
    host: host.docker.internal
    port: 22
    type: tcp

services:
  - name: Example HTTP
    url: https://example.com
    type: http
